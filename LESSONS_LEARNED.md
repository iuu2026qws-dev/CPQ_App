# CPQ 项目 — Lessons Learned（踩坑记录）

> 最后更新：2026-07-16 | 维护人：码农
>
> ⚠️ 同类问题发生 ≥2 次即录入本文档，作为团队永久避坑指南。

---

## 🔴 首要原则：完成标准（2026-07-16 确立）

> **一个功能"完成"的标准不是后端+前端代码写完，而是要确保：**
>
> **1. 用户场景设计 → 达到设计要求**
> **2. 用户交互功能 → 达到设计要求**
> **3. 所有用户可操作的功能点 → 全部可用**
>
> **三层全部达标，才算完成。缺任何一层，不算。**

示例：审批规则配置功能

| 维度 | 设计要求 | 实际 | 判定 |
|------|---------|------|:--:|
| 后端 API | CRUD 接口齐全 | ✅ 已完成 | — |
| 前端页面 | `ApprovalRuleConfig.vue` 可视化管理 | ❌ 没做 | — |
| **用户场景** | 管理员能登录→配置审批链→保存→规则生效 | ❌ 整个场景不存在 | ❌ 未完成 |
| **用户交互** | 拖拽编排审批链、可视化预览流转 | ❌ 交互功能为 0 | ❌ 未完成 |

→ 即使后端+API层 100%，只要用户场景和用户交互没达标，功能就是未完成。

---

## LL-001：审批规则配置页缺失 — 违反完成标准

**发现日期：** 2026-07-16
**影响范围：** 审批管理模块（D05 数据域）
**严重程度：** 🔴 高
**违反原则：** 首要原则 — 完成标准

### 现象

客户问「多级审批链在哪里维护？」— 答案：数据库手写 JSON。设计文档中规划的 `ApprovalRuleConfig.vue` 从未开发。

### 违规点

| 完成标准维度 | 状态 |
|------------|:--:|
| 后端代码 | ✅ |
| 前端代码 | ❌ |
| **用户场景**（管理员配置审批规则） | ❌ |
| **用户交互**（可视化编排审批链） | ❌ |

### 根因

团队以技术分层（DB→Controller→API）标记进度，而非以用户场景（管理员配置审批规则）作为完成标准。API 返回 200 就被当成功能完成，用户交互和用户场景压根没纳入验收。

### 直接教训

> 前后端代码写完只是完成了工程实现。用户场景设计和用户交互功能全部达到设计要求，才算完成。

---

## LL-002：产品目录搜索 — axios 拦截器解包不一致

**发现日期：** 2026-07-16
**影响范围：** 产品目录页
**严重程度：** 🔴 高（P0 Bug，客户已报修）

### 现象

搜索产品编码 `EVE-LF280K` 提示「未找到」，但产品就在列表中，后端 API 也返回了正确数据。

### 根因

CPQ 项目 axios 拦截器存在**两种响应解包模式混用**：

| API 类型 | 后端响应 | 拦截器解包后 | 组件期望 |
|---------|---------|------------|---------|
| 分页查询 `/list` | `{rows, total}` | `{rows, total}` | `res.rows` |
| 搜索查询 `/search` | `{code, data}` | `{code, data}` | `res.data` |

`ProductCatalog.vue` 中 `handleSearch()` 混用了两种模式：列表加载用 `data?.rows || data` 做了解包兼容，但搜索接口直接用 `models.length` 判断，没做解包。

### 直接教训

> **axios 拦截器的响应解包规则必须在项目初期明确定义并全员遵守，不能每个组件各写一套。**

### 改进措施

1. 全局统一 axios 拦截器响应解包逻辑
2. 所有列表/搜索接口统一为 `{rows, total}` 格式
3. 代码评审时检查所有 `request.get()` 调用的响应解包是否一致

---

## LL-003：配置规则 CSP 执行引擎缺失 — 运行时代码断层

**发现日期：** 2026-07-16
**影响范围：** 配置引擎模块（D03，Configurator）
**严重程度：** 🔴 高
**违反原则：** 首要原则 — 完成标准

### 现象

- `cpq_config_rule` 表里有规则数据（conditionExpr / actionExpr）
- `ConfigRuleManager.vue` 管理页面能正常 CRUD
- 但销售在 Configurator 里选属性时，规则完全不生效
- 互斥属性不会禁用、推荐值不会出现、无关属性不会隐藏

### 违规点

| 完成标准维度 | 状态 | 说明 |
|------------|:--:|------|
| 数据层（DDL + CRUD） | ✅ | 表建好了，管理页面能录入 |
| 交互层（管理页面） | ✅ | `ConfigRuleManager.vue` 可 CRUD |
| **运行时（CSP 引擎）** | ❌ | 不存在 — 规则从未被读取和执行 |
| **用户场景**（销售选属性→规则自动生效） | ❌ | 整个运行时链路是断的 |

### 根因

**开发顺序错了。** 按「数据 → 管理」做了，漏了「运行时」。

```
实际：DDL → CRUD → 管理页面 ✅  → 前端配置器 ✅  → 执行引擎 ❌
正确：用户场景 → 执行引擎 → DDL → 管理页面 → 前端联调
```

三层功能都有三层实现，但团队把前两层（数据、交互）做完了就认为完成了，漏了第三层（运行时）：
- 数据层：存什么，表结构，API CRUD
- 交互层：用户怎么录入、查看、编辑
- **运行时**：在业务流程中，什么时候读、怎么算、结果怎么反馈 ← 最容易被漏

根本原因是**没有人对"运行时"负责** — 因为运行时最难验收（没法在 Swagger 里测 CSP 引擎，也没法截图"属性被正确禁用了"的 API 响应）。

### 直接教训

> **数据能录入 ≠ 功能可用。任何一个功能都必须跑通完整的运行时链路（触发→读取→运算→反馈），才算完成。**

### 改进措施

1. 所有功能验收必须包含"运行时验证"：模拟用户操作 → 确认规则被读取 → 确认结果正确反馈
2. 开发顺序必须先定"谁在什么时候调这个功能"，再建表和写管理页面
3. CSP 引擎作为 P0 任务纳入开发清单，与前端 Configurator 联调通过后方可交付

---

## 记录模板

新教训按以下模板追加：

```markdown
## LL-XXX：标题

**发现日期：** YYYY-MM-DD
**影响范围：** 模块/页面
**严重程度：** 🔴高 / 🟡中 / 🟢低

### 现象
（用户看到的问题）

### 根因
（技术或流程层面的原因）

### 直接教训
> 一句话总结

### 改进措施
1. ...
2. ...
```
