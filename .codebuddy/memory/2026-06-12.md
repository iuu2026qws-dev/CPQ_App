# 2026-06-12

## CPQ Portal 配置规则 - "适用产品ID" 改为 Lookup 组件
- 将 `ConfigRuleManager.vue` 中"适用产品ID"字段从 `el-input-number` 数字输入框改为 `ModelLookup` 组件
- `ModelLookup` 组件位于 `@/components/cpq/ModelLookup.vue`，支持远程搜索产品名称/编码，已在 PriceBookList、ChannelPriceList、BundleManager 等多处复用
- 字段标签从"适用产品ID"改为"适用产品"，placeholder 改为"搜索产品，空=全局规则"
- `form.modelId` 类型为 `number | undefined`，与 `ModelLookup` 的 `modelValue: number | null` 兼容
