# 🛑 接手指令 — 先读，别干活

> 这份文档是你（新 Agent）接手 CPQ 项目后**第一份、也是唯一一份**需要执行的操作指南。
> **在完成本指令全部步骤之前，禁止修改任何代码。**

---

## 第一步：读完以下 5 份文档（约 30 分钟）

按顺序读，每读完一份在 `[ ]` 里打 `[x]`：

- [ ] **CPQ_HANDOVER.md** — 交接主文档，项目全貌一张图
- [ ] **STATUS.md** — 当前状态：什么做完了、什么没做、什么坏了
- [ ] **LESSONS_LEARNED.md** — 前人踩过的坑，别再踩
- [ ] **dev_policy.md** — 开发铁律（axios 解包、Snowflake ID、status 字段）
- [ ] **001_Product Design Docs/CPQ_开发任务清单.md** — 13 项待办任务

---

## 第二步：回答以下 5 个问题（把答案写在下面）

读完文档后，在下方填写答案，然后提交到仓库作为接手确认：

1. **项目是做什么的？**（一句话）
   >
2. **技术栈是什么？**（列出后端 + 前端 + 数据库）
   >
3. **当前 Git 分支是什么？最新 commit 是什么？**
   >
4. **优先级最高的 3 个待办任务是什么？**
   >
5. **开发铁律中哪一条最容易踩坑？**
   >

---

## 第三步：验证环境（只查不改）

```bash
# 在项目目录下执行，只输出结果，不要安装任何东西：

java -version          # JDK 版本
node -v                # Node 版本
mvn --version 2>/dev/null || echo "Maven 未安装"
git status             # 确认仓库状态
```

---

## 第四步：向交接人反馈

将以下信息发给交接人：

```
✅ CPQ 项目接手确认

1. 项目理解：[一句话]
2. 环境状态：JDK __ / Node __ / Maven __
3. 已读完 5 份文档
4. 当前分支：__
5. 下一步待确认事项：[如有]
```

---

**收到"继续"指令之前，只看不写，只读不改。**
